package spring.annotations.core;

import org.springframework.context.annotation.Configuration;

/**
 * @Bean This annotation is used at the method level. The @Bean annotation works with @Configuration to
 * create Spring beans. As mentioned earlier, @Configuration  will have methods to instantiate
 * and configure dependencies. Such methods will be annotated with @Bean. The method annotated
 * with this annotation works as the bean ID, and it creates and returns the actual bean.
 */
@Configuration
public class At_Bean {

    //@Bean

    //@Bean
}
